/// Single source of truth for the tool version string.
/// Update this value when cutting a new release — it is referenced by the CLI and all reporters.
public enum ToolVersion {
    public static let current = "1.2.0"
}
